shared:
  image: node14

annotations:
  screwdriver.cd/chainPR: true
 
jobs:
  build1:
    requires: [ ~commit, ~pr ]
    steps:
      - hello: echo "Hello build1"
  build2:
    requires: [ build1 ]
    steps:
      - hello: echo "Hello build2"
